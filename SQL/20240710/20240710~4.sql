SELECT * FROM STUDENT;
SELECT * FROM T_EMP;
-- 유용한 함수(그룹 함수)
-- 평균(AVG)
SELECT AVG(STU_HEIGHT)
FROM STUDENT;

-- 합(SUM)
SELECT SUM(PAY)
FROM T_EMP;

-- 최대값
SELECT MAX(PAY)
FROM T_EMP;

-- 최소값
SELECT MIN(PAY)
FROM T_EMP;